# 开发环境 Dockerfile
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 安装 MySQL 客户端
RUN apk add --no-cache mysql-client

# 复制 package.json 和 yarn.lock
COPY package.json yarn.lock ./

# 安装依赖（包括开发依赖）
RUN yarn install

# 复制项目文件
COPY . .

# 暴露端口
EXPOSE 3000

# 开发模式启动
CMD ["yarn", "dev"]
